import GameBoard from './components/GameBoard';

export default function App() {
  return (
    <main className="min-h-screen bg-indigo-950 flex items-center justify-center p-safe">
      <GameBoard />
    </main>
  );
}
